# names = ["john", "alice", "bob"]

# upper_names = [name.upper() for name in names]

# print(upper_names)
 
# names = ["john", "alice", "bob"]

# upper_names = [name.upper() for name in names]

# print(upper_names)
 

numbers = range(1, 11)
evens = [n for n in numbers if n % 2 == 0]
print(evens)